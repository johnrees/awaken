.holder
  .left
  .polaroid
    .box
    .side
    .image
    .side
    .text
      %p
        %span.kind Kind:
        %span.name Name
      %p
        %span.client Client:
        %span.client_name Client
  .right

  %ul#video-thumbs
    - 3.times do
      - @videos.each_with_index do |video, index|
        %li.thumb
          = link_to video, data: { kind: video.kind, name: video.name, client: video.client } do
            = image_tag video.thumbnail
